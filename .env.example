# Optional GitHub token for higher API rate limits.
GITHUB_TOKEN=
# Optional Gemini structured project-depth judgment. Deterministic scoring works without it.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
GITHUB_RECENCY_DAYS=90
